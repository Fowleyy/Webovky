@model List<Semestralka.Models.User>

@{
    ViewData["Title"] = "Administrace – Uživatelé";
}

<h2 class="fw-bold mb-4">Administrace – Uživatelé</h2>

<div class="mb-3">
    <form method="get" class="d-flex gap-2">
        <input name="search" class="form-control" placeholder="Vyhledat uživatele..."
               value="@ViewBag.Search" />
        <button class="btn btn-primary">Hledat</button>
    </form>
</div>

<div style="max-height: 500px; overflow-y: auto; border: 1px solid #ddd;">
    <table class="table table-hover">
        <thead class="table-light">
            <tr>
                <th>Jméno</th>
                <th>Email</th>
                <th>Akce</th>
            </tr>
        </thead>

        <tbody>
        @foreach (var u in Model)
        {
            <tr>
                <td>@u.FullName</td>
                <td>@u.Email</td>
                <td class="d-flex gap-2">
                    <a href="/admin/calendar/@u.Id" class="btn btn-sm btn-purple">
                        📅 Zobrazit kalendář
                    </a>

                    <a href="/admin/delete-user/@u.Id"
                       class="btn btn-sm btn-danger"
                       onclick="return confirm('Opravdu smazat tento účet?')">
                        🗑 Smazat
                    </a>
                </td>
            </tr>
        }
        </tbody>
    </table>
</div>
